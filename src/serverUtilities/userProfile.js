"use strict";
const userProfile = {
    // I want user profile separate from user auth!
    username: username,
    notes: [
        // We make an array of notes created by the user. It can save all the objectIs of notes.
    ],
    dictionaryInputs: [
        // ID to dictionary inputs.
    ],
    bookmarks: [
        // Id to bookmarked pages.
    ]
}
