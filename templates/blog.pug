extends ./layout.pug 
block blogPosts
    div.blogLandingpage
        each post in posts
            div.blogLandingpagePost
                h1 
                    a(href="/blog/" + postName[posts.indexOf(post)]) #{post.title}
                p   #{post.description}
                p.postDate #{post.date}
        
        